<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
            Welcome!
            {% endblock %}
        </title>

        {# estilos #}
        <link rel="stylesheet" href="{{ absolute_url(asset('css/bootstrap.min.css')) }}"/>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"/>
    </head>
    <body>
    <div class="container">
        {% block body %}
        {% endblock %}
    </div>

    {# scripts #}
    <script src="{{ absolute_url(asset('js/bootstrap.bundle.min.js')) }}"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="{{ absolute_url(asset('js/script.js')) }}"></script>

    {# Bloque para que las otras páginas puedan meter JS extra #}
    {% block javascripts %}
    {% endblock %}
    
    </body>
</html>
